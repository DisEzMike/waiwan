// Mobile picker stub: kept for compatibility but does not depend on image_picker here.
// The real mobile picker is used directly in the ProfileUploadScreen.
import 'picker_interface.dart';

Future<PickedImage?> pickImage() async {
  // Not implemented: use ImagePicker directly in screens.
  return null;
}
